spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/test # H2 ?????? ?? URL
    username: sa                        # ?????? ??? ??
    driver-class-name: org.h2.Driver

    # ???? yml??? ???? ??
    # true : url? ????, ????? ??? DB ??? name???? ??
    #generate-unique-name: true
    #name: nobody

  jpa:
    hibernate:
      ddl-auto: create # ??? ?? ????
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true                           # SQL ??? Hibernate ?? ?? ?? ??
        highlight_sql: true                              # ??? ? ?? ??
        generate_statistics: true                        # Hibernate? ??? SQL ?? ?? ? ?? ??? ??????.
        dialect: org.hibernate.dialect.H2Dialect
  #      dialect: org.hibernate.dialect.Oracle12cDialect
  #      dialect: org.hibernate.dialect.PostgreSQLDialect
  #      dialect: org.hibernate.dialect.MySQL5Dialect
  #      dialect: org.hibernate.dialect.SQLServer2012Dialect

# =========================
# ? MyBatis ??
# =========================
mybatis:
  type-aliases-package: hello.itemservice.domain   # MyBatis XML?? ??? ?? ?? ???? ?? ??? ????? Class?? ????? ?
  configuration:
    map-underscore-to-camel-case: true             # DB? ?????(_)? ?? ???? ?? ??


# =========================
# ? ?? ?? (SQL ? ???? ??)
# =========================

logging:
  level:
    # ?? Spring JDBC ? ???? ??
    org.springframework.jdbc: DEBUG                                         # JDBC ?? ??? ?? ?? ex) DEBUG: Executing SQL statement [SELECT * FROM users WHERE id=?]
    org.springframework.jdbc.datasource.DataSourceTransactionManager: DEBUG # ????? ???? ??? ?? ex) DEBUG: Began JDBC transaction on Connection [conn1]
    org.springframework.test.context.transaction: TRACE                     # ??? ??? ???? ?? ?? ex) TRACE: Began transaction (transaction manager [JpaTransactionManager])
    org.springframework.transaction.interceptor: TRACE                      # ???? ????? ?? ?? ?? ex) TRACE: Getting transaction for [com.example.service.MyService.saveData]
    org.springframework.orm.jpa.JpaTransactionManager: DEBUG                # JPA ???? ??? ?? ex) DEBUG: Committing JPA transaction on EntityManager [em1]

    # ?? MyBatis Repository ??
    hello.itemservice.repository.mybatis: TRACE                   # MyBatis Repository? SQL ?? ? ?? ?? ?? ex) TRACE: Querying MyBatis mapper for method [selectUserById]

    # ?? Hibernate SQL ? ???? ??? ??
    org.hibernate.SQL: DEBUG                                      # SQL ???? ex) DEBUG: select * from users where id=?
    org.hibernate.orm.jdbc.bind: TRACE                            # (????? 3.x??)SQL ???? ??? ??(JPA? ??JDBC ??? ??? ????) ex) TRACE [org.hibernate.orm.jdbc.bind] - binding parameter [1] = 'apple' (VARCHAR)
    org.hibernate.orm.jdbc.extract: TRACE                         # JDBC ?? ?? ?? (?????? ??)
    org.hibernate.type: TRACE                                     # Hibernate? ??? ?? ?? ?? ?? ex) TRACE [org.hibernate.type] - Binding parameter [1] as [VARCHAR] - [apple] (????? 2.x??)
    org.hibernate.resource.transaction: DEBUG                     # Hibernate? ???? ??? ?? ?? ex) DEBUG: Initiating transaction after acquiring JDBC connection
#    org.hibernate.type.descriptor.sql: TRACE                      # (????? 2.x??)SQL ???? ??? ?? ?? ?? (???? ??)             ex) TRACE [org.hibernate.type.descriptor.sql] - Binding parameter [1] as [VARCHAR] - [apple]
#    org.hibernate.type.descriptor.sql.BasicBinder: TRACE          # (????? 2.x??)SQL ???? ???? ?? ?? ?? (????? ??? ??) ex) TRACE [org.hibernate.type.descriptor.sql.BasicBinder] - binding parameter [1] -> [apple] (VARCHAR)
